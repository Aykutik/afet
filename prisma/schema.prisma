generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Account {
  id                String  @id
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?
  User              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId], map: "Account_userId_fkey")
}

model Session {
  id           String   @id
  sessionToken String   @unique
  userId       String
  expires      DateTime
  User         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId], map: "Session_userId_fkey")
}

model User {
  id            String    @id
  name          String?
  pertcno       String?   @unique(map: "User_email_key") @db.VarChar(11)
  emailVerified DateTime?
  image         String?
  Account       Account[]
  Session       Session[]
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model auth_group {
  id                     Int                      @id @default(autoincrement())
  name                   String                   @unique(map: "name") @db.VarChar(150)
  auth_group_permissions auth_group_permissions[]
  auth_user_groups       auth_user_groups[]
}

model auth_group_permissions {
  id              BigInt          @id @default(autoincrement())
  group_id        Int
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onUpdate: Restrict, map: "auth_group_permissio_permission_id_84c5c92e_fk_auth_perm")
  auth_group      auth_group      @relation(fields: [group_id], references: [id], onUpdate: Restrict, map: "auth_group_permissions_group_id_b120cbf9_fk_auth_group_id")

  @@unique([group_id, permission_id], map: "auth_group_permissions_group_id_permission_id_0cd325b0_uniq")
  @@index([permission_id], map: "auth_group_permissio_permission_id_84c5c92e_fk_auth_perm")
}

model auth_permission {
  id                         Int                          @id @default(autoincrement())
  name                       String                       @db.VarChar(255)
  content_type_id            Int
  codename                   String                       @db.VarChar(100)
  auth_group_permissions     auth_group_permissions[]
  django_content_type        django_content_type          @relation(fields: [content_type_id], references: [id], onUpdate: Restrict, map: "auth_permission_content_type_id_2f476e4b_fk_django_co")
  auth_user_user_permissions auth_user_user_permissions[]

  @@unique([content_type_id, codename], map: "auth_permission_content_type_id_codename_01ab375a_uniq")
}

model auth_user {
  id                         Int                          @id @default(autoincrement())
  password                   String?                      @db.VarChar(128)
  last_login                 DateTime?                    @db.DateTime(6)
  is_superuser               Boolean                      @default(false)
  username                   String                       @unique(map: "username") @default("") @db.Char(50)
  first_name                 String                       @default("") @db.Char(50)
  last_name                  String                       @default("") @db.Char(50)
  email                      String?                      @db.Char(50)
  is_staff                   Boolean                      @default(true)
  is_active                  Boolean?                     @default(true)
  date_joined                DateTime?                    @db.DateTime(6)
  auth_user_groups           auth_user_groups[]
  auth_user_user_permissions auth_user_user_permissions[]
  django_admin_log           django_admin_log[]
}

model auth_user_groups {
  id         BigInt     @id @default(autoincrement())
  user_id    Int
  group_id   Int
  auth_group auth_group @relation(fields: [group_id], references: [id], onUpdate: Restrict, map: "auth_user_groups_group_id_97559544_fk_auth_group_id")
  auth_user  auth_user  @relation(fields: [user_id], references: [id], onUpdate: Restrict, map: "auth_user_groups_user_id_6a12ed8b_fk_auth_user_id")

  @@unique([user_id, group_id], map: "auth_user_groups_user_id_group_id_94350c0c_uniq")
  @@index([group_id], map: "auth_user_groups_group_id_97559544_fk_auth_group_id")
}

model auth_user_user_permissions {
  id              BigInt          @id @default(autoincrement())
  user_id         Int
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onUpdate: Restrict, map: "auth_user_user_permi_permission_id_1fbb5f2c_fk_auth_perm")
  auth_user       auth_user       @relation(fields: [user_id], references: [id], onUpdate: Restrict, map: "auth_user_user_permissions_user_id_a95ead1b_fk_auth_user_id")

  @@unique([user_id, permission_id], map: "auth_user_user_permissions_user_id_permission_id_14a6b632_uniq")
  @@index([permission_id], map: "auth_user_user_permi_permission_id_1fbb5f2c_fk_auth_perm")
}

model avans {
  perkod String?   @db.VarChar(11)
  tarih  DateTime? @db.Date
  tutar  Float?
  taksit Int?
  kalan  Float?
  durum  String?   @default("Devam Ediyor") @db.VarChar(15)
  id     Int       @unique(map: "id") @default(autoincrement())
}

model avans_ayr_nt_ {
  perkod  String? @db.VarChar(11)
  avansid String? @db.VarChar(20)
  d_nem   String? @map("dönem") @db.VarChar(15)
  taksit  String? @db.VarChar(6)
  tutar   Float?
  durum   String? @default("1") @db.VarChar(12)
  dendi   String? @default("false") @map("ödendi") @db.VarChar(50)
  id      Int     @unique(map: "id") @default(autoincrement())

  @@map("avans_ayrıntı")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model ayarlar_b_r_t {
  id                        Int
  pay1                      Float @default(0)
  pay2                      Float @default(0)
  pay3                      Float @default(0)
  pay4                      Float @default(0)
  pay5                      Float @default(0)
  dilim1tutar               Float @default(0)
  dilim2tutar               Float @default(0)
  dilim3tutar               Float @default(0)
  dilim4tutar               Float @default(0)
  fazlas_1                  Float @default(0) @map("fazlası1")
  fazlas_2                  Float @default(0) @map("fazlası2")
  fazlas_3                  Float @default(0) @map("fazlası3")
  fazlas_4                  Float @default(0) @map("fazlası4")
  asVer_sTutar              Float @default(0) @map("asVerİsTutar")
  i_ciSSK                   Float @default(0) @map("işciSSK")
  i_cii_sizlik              Float @default(0) @map("işciişsizlik")
  i_verenssk                Float @default(0) @map("işverenssk")
  i_vereni_sizlik           Float @default(0) @map("işverenişsizlik")
  damgaVergisi              Float @default(0)
  tavanB_r_tAsgari_cret     Float @default(0) @map("tavanBürütAsgariÜcret")
  tavanB_r_tAsgari_cret_k_m Float @default(0) @map("tavanBürütAsgariÜcret_küm")
  engelliKademe1            Float @default(0)
  engelliKademe2            Float @default(0)
  engelliKademe3            Float @default(0)

  @@index([id], map: "id")
  @@map("ayarlar_bürüt")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model ayarlar_filtreler {
  filtread_    String? @map("filtreadı") @db.Char(50)
  id           Int     @default(autoincrement())
  filtreKolonu String? @db.Char(50)
  filtreDe_eri String? @map("filtreDeğeri") @db.Char(50)

  @@index([id], map: "id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model ayarlar_giri__veritaban_ {
  server       String? @db.Char(100)
  veritaban_   String? @map("veritabanı") @db.Char(100)
  kullan_c_Ad_ String? @map("kullanıcıAdı") @db.Char(100)
  parola       String? @db.Char(100)
  id           Int     @default(autoincrement())

  @@index([id], map: "id")
  @@map("ayarlar_giriş_veritabanı")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model ayarlar_kullan_c__giri_ {
  kullan_c_Ad_     String? @map("kullanıcıAdı") @db.Char(100)
  bilgisayarID     String? @db.Char(100)
  kullan_c_AdSoyad String? @map("kullanıcıAdSoyad") @db.Char(100)
  beniHat_rla      Int?    @map("beniHatırla")
  pertel           String? @db.Char(20)
  parola           String? @db.Char(50)
  id               Int     @default(autoincrement())

  @@index([id], map: "id")
  @@map("ayarlar_kullanıcı_giriş")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model ayarlar_mesaj {
  ayar   String? @db.Char(50)
  de_er  String? @map("değer") @db.Char(35)
  aktif  String? @db.Char(1)
  saat   String? @db.Char(2)
  dakika String? @db.Char(2)
  id     Int     @default(autoincrement())

  @@index([id], map: "id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model ayarlar_mesaj_do_umG_n_Mesaj_ {
  mesaj String? @db.Char(250)
  aktif Int?
  id    Int     @default(autoincrement())

  @@index([id], map: "id")
  @@map("ayarlar_mesaj_doğumGünüMesajı")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model ayarlar_mesaj__nemliG_nMesajlar_ {
  g_n        String? @map("gün") @db.Char(2)
  ay         String? @db.Char(2)
  nemliG_n   String? @map("önemliGün") @db.Char(75)
  mesaj      String? @db.Char(250)
  filtre     String? @db.Char(25)
  g_nderildi String? @default("0") @map("gönderildi") @db.Char(1)
  aktif      String? @default("0") @db.Char(1)
  id         Int     @default(autoincrement())

  @@index([id], map: "id")
  @@map("ayarlar_mesaj_önemliGünMesajları")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model ayarlar_mesajlar_giden {
  pertcno String?   @db.VarChar(11)
  mesaj   String?   @db.VarChar(500)
  tarih   DateTime? @db.Date
  saat    DateTime? @db.Time(0)
  t_r     String?   @map("tür") @db.Char(60)
  id      Int       @default(autoincrement())

  @@index([id], map: "id")
  @@ignore
}

model aykut {
  sesssion_id String  @id @db.VarChar(128)
  expires     Int     @db.UnsignedInt
  data        String? @db.MediumText
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb {
  al__ma_ekli          String? @map("çalışmaşekli") @db.VarChar(30)
  al__madurumu         String? @map("çalışmadurumu") @db.VarChar(30)
  engeli               String? @db.VarChar(30)
  evethay_r            String? @map("evethayır") @db.VarChar(30)
  askerlikdurumu       String? @db.VarChar(30)
  e_itimdurumu         String? @map("eğitimdurumu") @db.VarChar(30)
  kangrubu             String? @db.VarChar(30)
  servis               String? @db.VarChar(30)
  durak                String? @db.VarChar(30)
  kalfal_kustal_k      String? @map("kalfalıkustalık") @db.VarChar(30)
  kalfal_kustal_kdurum String? @map("kalfalıkustalıkdurum") @db.VarChar(30)
  k_s_m                String? @map("kısım") @db.VarChar(30)
  b_l_m                String? @map("bölüm") @db.VarChar(30)
  birim                String? @db.VarChar(30)
  g_revi               String? @map("görevi") @db.VarChar(0)
  medenihali           String? @db.VarChar(0)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_adg {
  adg String? @db.VarChar(0)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_belge {
  meslek String? @db.VarChar(50)
  belge  String? @db.VarChar(20)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_belge_durum {
  belge_durum String? @db.VarChar(50)
  belge       String? @db.VarChar(30)
  belge2      String? @db.VarChar(30)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_birim {
  birim String? @db.VarChar(20)
  b_l_m String? @map("bölüm") @db.VarChar(20)
  id    Int     @default(autoincrement())

  @@index([id], map: "id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_br_amir {
  br_amir     String? @db.VarChar(25)
  br_amir_tel String? @db.VarChar(20)
  birim       String? @db.VarChar(20)
  b_l_m       String? @map("bölüm") @db.VarChar(20)

  @@ignore
}

model cmb_b_l_m {
  k_s_m String? @map("kısım") @db.VarChar(20)
  b_l_m String? @map("bölüm") @db.VarChar(50)
  mola  Int?    @db.TinyInt
  id    Int     @id @default(autoincrement())

  @@map("cmb_bölüm")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_ehliyet {
  id    Int     @default(autoincrement()) @db.TinyInt
  sinif String? @db.VarChar(25)
  src   String? @db.VarChar(25)

  @@index([id], map: "id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_g_rev {
  g_revi String? @map("görevi") @db.VarChar(60)
  Birim  String? @db.VarChar(20)
  id     Int     @default(autoincrement())

  @@index([id], map: "id")
  @@map("cmb_görev")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_hizmetal_m__firmalar {
  id      Int     @default(autoincrement())
  firmaad String? @db.VarChar(50)
  vergino String? @db.VarChar(50)
  tel     String? @db.VarChar(50)
  adres   String? @db.VarChar(250)
  email   String? @db.VarChar(50)
  web     String? @db.VarChar(50)

  @@index([id], map: "id")
  @@map("cmb_hizmetalımı_firmalar")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_izin {
  d    Int?    @map("ıd") @db.TinyInt
  izin String? @db.VarChar(25)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_k_s_m {
  d     Int?    @map("ıd") @db.TinyInt
  k_s_m String? @map("kısım") @db.VarChar(10)

  @@map("cmb_kısım")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_sekil {
  sekil String? @db.VarChar(10)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model cmb_servis {
  servis String? @db.VarChar(0)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model deneme {
  deneme String? @db.VarChar(50)

  @@ignore
}

model django_admin_log {
  id                  Int                  @id @default(autoincrement())
  action_time         DateTime             @db.DateTime(6)
  object_id           String?              @db.LongText
  object_repr         String               @db.VarChar(200)
  action_flag         Int                  @db.UnsignedSmallInt
  change_message      String               @db.LongText
  content_type_id     Int?
  user_id             Int
  django_content_type django_content_type? @relation(fields: [content_type_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "django_admin_log_content_type_id_c4bce8eb_fk_django_co")
  auth_user           auth_user            @relation(fields: [user_id], references: [id], onUpdate: Restrict, map: "django_admin_log_user_id_c564eba6_fk_auth_user_id")

  @@index([content_type_id], map: "django_admin_log_content_type_id_c4bce8eb_fk_django_co")
  @@index([user_id], map: "django_admin_log_user_id_c564eba6_fk_auth_user_id")
}

model django_content_type {
  id               Int                @id @default(autoincrement())
  app_label        String             @db.VarChar(100)
  model            String             @db.VarChar(100)
  auth_permission  auth_permission[]
  django_admin_log django_admin_log[]

  @@unique([app_label, model], map: "django_content_type_app_label_model_76bd3d3b_uniq")
}

model django_migrations {
  id      BigInt   @id @default(autoincrement())
  app     String   @db.VarChar(255)
  name    String   @db.VarChar(255)
  applied DateTime @db.DateTime(6)
}

model django_session {
  session_key  String   @id @db.VarChar(40)
  session_data String   @db.LongText
  expire_date  DateTime @db.DateTime(6)

  @@index([expire_date], map: "django_session_expire_date_a5c62663")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model k_s_m {
  k_s_m String? @map("kısım") @db.VarChar(10)

  @@map("kısım")
  @@ignore
}

model log_mesaj {
  cihazAd_     String?   @map("cihazAdı") @db.Char(25)
  kullan_c_Ad_ String?   @map("kullanıcıAdı") @db.Char(25)
  mesaj        String?   @db.VarChar(500)
  al_c_Tel     String?   @map("alıcıTel") @db.Char(20)
  kod          String?   @db.Char(15)
  al_c_AdSoyad String?   @map("alıcıAdSoyad") @db.Char(30)
  al_c_TcNo    String?   @map("alıcıTcNo") @db.Char(15)
  durum        String?   @db.Char(150)
  tarih        DateTime? @db.Date
  saat         DateTime? @db.Time(0)
  id           Int       @unique(map: "id") @default(autoincrement())
}

model mesajlar_mesaj {
  t_r             String?   @map("tür") @db.Char(50)
  al_c_           String?   @map("alıcı") @db.Char(11)
  al_c_adsoyad    String?   @map("alıcıadsoyad") @db.Char(50)
  tarih           DateTime? @db.Date
  saat            DateTime? @db.Time(0)
  okundu          Int       @default(0)
  silindi         Int       @default(0)
  silindi2        Int?      @default(0)
  konu            String?   @db.Char(100)
  mesaj           String?   @db.VarChar(800)
  g_nderen        String?   @map("gönderen") @db.Char(50)
  g_nderenadsoyad String?   @map("gönderenadsoyad") @db.Char(50)
  sahiptcno       String?   @db.Char(11)
  sahipadsoyad    String?   @db.Char(50)
  nem             Int?      @default(0) @map("önem")
  id              Int       @unique(map: "id") @default(autoincrement())
}

model native {
  barkod  String?   @db.VarChar(50)
  t_r     String?   @map("tür") @db.VarChar(50)
  boyut   String?   @db.VarChar(50)
  partino String?   @db.VarChar(50)
  serino  String?   @db.VarChar(50)
  tarih   DateTime? @db.Date
  saat    DateTime? @db.Time(0)
  id      Int       @unique(map: "id") @default(autoincrement())
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model perbilgi {
  adsoyad String? @db.Char(50)
  tcno    Float?  @db.Float

  @@ignore
}

model personel_avans {
  pertcno    String?   @db.VarChar(11)
  peradsoyad String?   @db.Char(50)
  tarih      DateTime? @db.Date
  tutar      String?   @db.Char(15)
  taksit     Int?
  kalan      String?   @db.VarChar(50)
  durum      String?   @default("Devam Ediyor") @db.VarChar(15)
  a__klama   String?   @map("açıklama") @db.Char(250)
  e          Int?
  id         Int       @unique(map: "id") @default(autoincrement())
}

model personel_avans_ayr_nt_ {
  pertcno    String? @db.VarChar(11)
  peradsoyad String? @db.VarChar(50)
  avansid    String? @db.Char(6)
  d_nem      String? @map("dönem") @db.VarChar(15)
  taksit     String? @db.VarChar(6)
  tutar      String? @db.VarChar(50)
  durum      String? @default("1") @db.VarChar(12)
  dendi      String? @default("false") @map("ödendi") @db.VarChar(50)
  id         Int     @unique(map: "id") @default(autoincrement())

  @@map("personel_avans_ayrıntı")
}

model personel_avans_istek {
  onay              Int?      @default(0)
  pertcno           String?   @db.VarChar(11)
  peradsoyad        String?   @db.VarChar(50)
  tarih             DateTime? @db.Date
  d_nemay           String?   @map("dönemay") @db.Char(2)
  d_nemy_l          String?   @map("dönemyıl") @db.Char(4)
  tutar             String?   @db.Char(15)
  taksit            Int?
  taksittutar       String?   @db.Char(10)
  kalan             String?   @db.VarChar(50)
  durum             String?   @default("Devam Ediyor") @db.VarChar(15)
  a__klama          String?   @map("açıklama") @db.Char(250)
  perbirimamiri     String?   @db.Char(100)
  perbirimamiritcno String?   @db.Char(11)
  onaylayan         String?   @db.Char(50)
  onaylayan2        String?   @db.Char(50)
  onaylayantcno     String?   @db.Char(50)
  onaylayantcno2    String?   @db.Char(50)
  onaylayana__klama String?   @map("onaylayanaçıklama") @db.Char(250)
  onaytarihi        DateTime? @db.Date
  g_nderildi        Int?      @default(0) @map("gönderildi")
  g_nderen          String?   @map("gönderen") @db.Char(50)
  g_nderentcno      String?   @map("gönderentcno") @db.Char(11)
  g_nderena__klama  String?   @map("gönderenaçıklama") @db.Char(250)
  g_ndermetarihi    DateTime? @map("göndermetarihi") @db.Date
  periban           String?   @db.Char(50)
  id                Int       @unique(map: "id") @default(autoincrement())
}

model personel_bes {
  perkod          String?   @db.VarChar(50)
  peradsoyad      String?   @db.VarChar(50)
  firma           String?   @db.VarChar(50)
  s_zle_meno      String?   @map("sözleşmeno") @db.VarChar(50)
  fontercihi      String?   @db.VarChar(50)
  giri_tarihi     DateTime? @map("giriştarihi") @db.Date
  k__tarihi       DateTime? @map("çıkıştarihi") @db.Date
  ba_lang__d_nemi String?   @map("başlangıçdönemi") @db.VarChar(7)
  biti_d_nemi     String?   @map("bitişdönemi") @db.VarChar(7)
  kesinti_t_r_    String?   @map("kesinti_türü") @db.VarChar(15)
  kesintioran_    String?   @map("kesintioranı") @db.VarChar(50)
  durum           Boolean?  @default(true)
  k_mkesinti      Float?    @default(0) @map("kümkesinti")
  k_may           Int?      @default(0) @map("kümay")
  id              Int       @unique(map: "id") @default(autoincrement())
}

model personel_bes__deme {
  perkod  String? @db.VarChar(10)
  besid   String? @db.VarChar(10)
  d_nem   String? @map("dönem") @db.VarChar(7)
  kesinti Float?
  id      Int     @unique(map: "id") @default(autoincrement())

  @@map("personel_bes_ödeme")
}

model personel_bildirim {
  personel       String?   @db.Char(12)
  bildirim       String?   @db.Char(250)
  t_r            String?   @map("tür") @db.Char(50)
  t_r_id         Int?      @map("tür_id")
  ilgilipersonel String?   @db.Char(50)
  okundu         Int?      @default(0)
  cevap          Int?
  tarih          DateTime? @db.DateTime(0)
  id             Int       @unique(map: "id") @default(autoincrement())
}

model personel_evrak {
  id             Int     @unique(map: "id") @default(autoincrement())
  evrak_t_r_     String? @map("evrak_türü") @db.VarChar(20)
  peradsoyad     String? @db.VarChar(50)
  pertcno        String? @db.VarChar(11)
  evrak_ad_      String? @map("evrak_adı") @db.VarChar(20)
  dosya_ad_      String? @map("dosya_adı") @db.VarChar(20)
  dosya_tarihi   String? @db.VarChar(20)
  gelen_giden    String? @db.VarChar(20)
  tutanak_nedeni String? @db.VarChar(20)
}

model personel_giriscikis {
  perkod             String?   @db.Char(10)
  pertcno            String?   @db.VarChar(11)
  peradsoyad         String?   @db.Char(50)
  tarih              DateTime? @db.Date
  perb_l_m           String?   @map("perbölüm") @db.Char(22)
  giris              DateTime? @db.Time(0)
  cikis              DateTime? @db.Time(0)
  tamam              Int?
  giris_eksik        DateTime? @db.Time(0)
  cikis_eksik        DateTime? @db.Time(0)
  eksik_mesai        DateTime? @db.Time(0)
  eksik_mesai_dec    Float     @default(0)
  tk                 Int?      @default(0)
  fazla_mesai        DateTime? @db.Time(0)
  fazla_mesai_dec    Float     @default(0)
  hi_fazla_mesai_dec Float     @default(0)
  hs_fazla_mesai_dec Float     @default(0)
  rt_fazla_mesai_dec Float     @default(0)
  by_fazla_mesai_dec Float     @default(0)
  durum              String?   @db.Char(55)
  dk                 Boolean?  @default(false)
  fm                 Boolean?  @default(false)
  fkontrol           String    @default("0") @db.VarChar(1)
  aciklama           String?   @db.Char(250)
  id                 Int       @unique(map: "id") @default(autoincrement())
}

model personel_giriscikis_onay {
  onay               Int       @default(0)
  onayland_          Int?      @default(0) @map("onaylandı")
  kapatan            String?   @db.Char(35)
  kapatana__klama    String?   @map("kapatanaçıklama") @db.Char(250)
  perkod             String?   @db.Char(10)
  pertcno            String?   @db.VarChar(11)
  peradsoyad         String?   @db.Char(50)
  perbirimamiri      String?   @db.Char(35)
  perbirimamiritcno  String?   @db.VarChar(11)
  perg_revyeri       String?   @map("pergörevyeri") @db.Char(50)
  tarih              DateTime? @db.Date
  giris              DateTime? @db.Time(0)
  cikis              DateTime? @db.Time(0)
  tamam              Int?
  giris_eksik        DateTime? @db.Time(0)
  cikis_eksik        DateTime? @db.Time(0)
  eksik_mesai        DateTime? @db.Time(0)
  eksik_mesai_dec    Float     @default(0)
  tk                 Int?      @default(0)
  fazla_mesai        DateTime? @db.Time(0)
  fazla_mesai_dec    Float     @default(0)
  hi_fazla_mesai_dec Float     @default(0)
  hs_fazla_mesai_dec Float     @default(0)
  rt_fazla_mesai_dec Float     @default(0)
  by_fazla_mesai_dec Float     @default(0)
  durum              String?   @db.Char(55)
  giris_onceki       DateTime? @db.Time(0)
  cikis_onceki       DateTime? @db.Time(0)
  durum_onceki       String?   @db.Char(250)
  aciklama_onceki    String?   @db.Char(250)
  dk                 Boolean?  @default(false)
  fm                 Boolean?  @default(false)
  fkontrol           String    @default("0") @db.VarChar(1)
  aciklama           String?   @db.Char(250)
  t_r                String?   @map("tür") @db.Char(50)
  id                 Int       @unique(map: "id") @default(autoincrement())
}

model personel_icra_ayr_nt_ {
  perkod            String?   @db.VarChar(10)
  t_r               String?   @map("tür") @db.VarChar(50)
  dosya_y_l_        String?   @map("dosya_yılı") @db.VarChar(5)
  dosya_no          String?   @db.VarChar(50)
  tebli__tarihi     DateTime? @map("tebliğ_tarihi") @db.Date
  alacakl_          String?   @map("alacaklı") @db.VarChar(150)
  alacakl__aciklama String?   @map("alacaklı_aciklama") @db.VarChar(200)
  banka_ad_         String?   @map("banka_adı") @db.VarChar(150)
  iban_no           String?   @db.VarChar(20)
  mahkeme_ad_       String?   @map("mahkeme_adı") @db.VarChar(150)
  icra_dairesi      String?   @db.VarChar(150)
  kesinti_y_zdesi   String?   @map("kesinti_yüzdesi") @db.VarChar(50)
  kesinti_sabit     Float?
  ncelik            Int?      @map("öncelik") @db.TinyInt
  ba_lama_d_nemi    String?   @map("başlama_dönemi") @db.VarChar(7)
  toplam_bor_       Float?    @map("toplam_borç")
  nceden_tahsil     Float?    @default(0) @map("önceden_tahsil")
  bordro_tahsil     Float?    @default(0)
  toplam_tahsil     Float?
  kalan_bor_        Float?    @map("kalan_borç")
  durum             Boolean?  @default(true)
  id                Int       @unique(map: "id") @default(autoincrement())

  @@map("personel_icra_ayrıntı")
}

model personel_icra__deme {
  icraid     String? @db.VarChar(5)
  perkod     String? @db.VarChar(10)
  t_r        String? @map("tür") @db.VarChar(20)
  dosya_y_l_ String? @map("dosya_yılı") @db.VarChar(4)
  dosya_no   String? @db.VarChar(10)
  d_nem      String? @map("dönem") @db.VarChar(7)
  kesinti    Float?
  id         Int     @unique(map: "id") @default(autoincrement())

  @@map("personel_icra_ödeme")
}

model personel_isg_sa_l_ktaramas_ {
  id             Int       @unique(map: "id") @default(autoincrement())
  peradsoyad     String?   @db.VarChar(50)
  firma          String?   @db.VarChar(50)
  tarih          DateTime? @db.Date
  y_l            String?   @map("yıl") @db.VarChar(4)
  pertcno        String?   @db.VarChar(11)
  perdo_umtarihi DateTime? @map("perdoğumtarihi") @db.Date
  ki_isay_s_     String?   @map("kişisayısı") @db.VarChar(2)
  akci_er        String    @default("0") @map("akciğer") @db.VarChar(1)
  odyo           String    @default("0") @db.VarChar(1)
  sft            String    @default("0") @db.VarChar(1)
  tamkan         String    @default("0") @db.VarChar(1)
  g_z            String    @default("0") @map("göz") @db.VarChar(1)
  kur_un         String    @default("0") @map("kurşun") @db.VarChar(1)
  fenol          String    @default("0") @db.VarChar(1)
  alt            String    @default("0") @db.VarChar(1)
  ast            String    @default("0") @db.VarChar(1)
  ak_            String    @default("0") @map("akş") @db.VarChar(1)
  kreatin        String    @default("0") @db.VarChar(1)
  re             String    @default("0") @map("üre") @db.VarChar(1)

  @@map("personel_isg_sağlıktaraması")
}

model personel_izin {
  perkod     String?   @db.Char(50)
  pertcno    String?   @db.Char(11)
  peradsoyad String?   @db.Char(30)
  y_l        String?   @map("yıl") @db.Char(4)
  ay         Int?
  tarih      DateTime? @db.Date
  devreden   Int       @default(0)
  eklenen    Int       @default(0)
  kkontrol   String    @default("0") @db.VarChar(1)
  kullanilan Int       @default(0)
  aciklama   String?   @db.Char(50)
  id         Int       @unique(map: "id") @default(autoincrement())
}

model personel_izin_toplam {
  id                Int       @unique(map: "id") @default(autoincrement())
  pertcno           String?   @db.VarChar(11)
  peradsoyad        String?   @db.Char(40)
  peri_egiri_tarihi DateTime? @map("perişegiriştarihi") @db.Date
  perb_l_m          String?   @map("perbölüm") @db.Char(30)
  devreden          Int       @default(0)
  eklenen           Int       @default(0)
  ocak              Int       @default(0)
  ubat              Int       @default(0) @map("şubat")
  mart              Int       @default(0)
  nisan             Int       @default(0)
  may_s             Int       @default(0) @map("mayıs")
  haziran           Int       @default(0)
  temmuz            Int       @default(0)
  a_ustos           Int       @default(0) @map("ağustos")
  eyl_l             Int       @default(0) @map("eylül")
  ekim              Int       @default(0)
  kas_m             Int       @default(0) @map("kasım")
  aral_k            Int       @default(0) @map("aralık")
  kullanilan        Int       @default(0)
  kalan             Int       @default(0)
  aktif             Int       @default(1)
  y_l               String    @default("") @map("yıl") @db.Char(4)
}

model personel_karts_zi_lem {
  id      Int       @unique(map: "id") @default(autoincrement())
  pertcno String?   @db.VarChar(11)
  saat    DateTime? @db.Time(0)
  tarih   DateTime? @db.Date
  cihazid String?   @db.Char(5)
  onay    Int       @default(0)

  @@map("personel_kartsızişlem")
}

model personel_kredikarti {
  id           Int     @unique(map: "id") @default(autoincrement())
  banka        String? @db.Char(250)
  kartismi     String? @db.Char(50)
  kartnumaras_ String? @map("kartnumarası") @db.Char(20)
  a__klama     String? @map("açıklama") @db.Char(200)
}

model personel_maa_ {
  id        Int       @unique(map: "id") @default(autoincrement())
  pertcno   String?   @db.Char(11)
  tarih     DateTime? @db.Date
  ncekimaa_ Decimal?  @map("öncekimaaş") @db.Decimal(20, 6)
  yenimaa_  Decimal?  @map("yenimaaş") @db.Decimal(20, 6)
  farktl    String?   @db.Char(50)
  farky_zde String?   @map("farkyüzde") @db.Char(50)
  a__klama  String?   @map("açıklama") @db.Char(250)

  @@map("personel_maaş")
}

model personel_native {
  barkod  String?   @db.VarChar(50)
  t_r     String?   @map("tür") @db.VarChar(50)
  boyut   String?   @db.VarChar(50)
  partino String?   @db.VarChar(50)
  serino  String?   @db.VarChar(50)
  tarih   DateTime? @db.DateTime(0)
  saat    DateTime? @db.Time(0)
  id      Int       @unique(map: "id") @default(autoincrement())
}

model personel_nobet {
  id         Int       @unique(map: "id") @default(autoincrement())
  ad         String?   @db.Char(50)
  tel        String?   @db.Char(50)
  adres      String?   @db.Char(250)
  tarif      String?   @db.Char(250)
  sehir      String?   @db.Char(50)
  latitude   String?   @db.Char(50)
  longitude  String?   @db.Char(50)
  tarih      DateTime? @db.Date
  saat       DateTime? @db.Time(0)
  g_ncelleme DateTime? @map("güncelleme") @db.Time(0)
}

model personel_personeller {
  id                  Int       @id @default(autoincrement())
  perkod              String    @default("") @db.Char(20)
  perkart             String?   @db.Char(15)
  perad               String?   @db.Char(40)
  persoyad            String?   @db.Char(30)
  peradsoyad          String?   @db.Char(50)
  pertcno             String?   @unique(map: "User_email_key") @db.VarChar(11)
  percinsiyet         String?   @db.Char(10)
  peruyru_u           String?   @map("peruyruğu") @db.Char(10)
  perbabaad_          String?   @map("perbabaadı") @db.Char(20)
  perceptelefonu      String?   @db.Char(15)
  peri_telefonu       String?   @map("periştelefonu") @db.Char(15)
  perdahili           String?   @db.Char(10)
  pereposta           String?   @db.Char(50)
  perdo_umtarihi      DateTime? @map("perdoğumtarihi") @db.Date
  perdo_umyeri        String?   @map("perdoğumyeri") @db.Char(15)
  peraskerlik         String?   @db.Char(15)
  permedenihali       String?   @db.Char(15)
  pere_durumu         String?   @map("pereşdurumu") @db.Char(15)
  per_ocuk            String?   @map("perçocuk") @db.VarChar(2)
  per__renimdurumu    String?   @map("peröğrenimdurumu") @db.Char(30)
  permezunokul        String?   @db.Char(50)
  permezunokulb_l_m   String?   @map("permezunokulbölüm") @db.Char(50)
  peri_egiri_tarihi   DateTime? @map("perişegiriştarihi") @db.Date
  perk_demtarihi      DateTime? @map("perkıdemtarihi") @db.Date
  peri_ten__k__tarihi DateTime? @map("periştençıkıştarihi") @db.Date
  peradres            String?   @db.Char(250)
  peril               String?   @db.Char(50)
  peril_e             String?   @map("perilçe") @db.Char(50)
  perkangrubu         String?   @db.Char(10)
  perk_s_m            String?   @map("perkısım") @db.Char(50)
  perb_l_m            String?   @map("perbölüm") @db.Char(50)
  perbirim            String?   @db.Char(50)
  perbirimamiri       String?   @db.Char(50)
  perbirimamiriperkod String?   @db.Char(10)
  perbirimamiritcno   String?   @db.Char(11)
  perg_rev            String?   @map("pergörev") @db.Char(50)
  perehliyet          String?   @db.Char(20)
  perforkliftehliyeti Int?      @db.TinyInt
  persrc              String?   @db.Char(50)
  permykdurumu        String?   @db.Char(50)
  permykalan          String?   @db.Char(50)
  permykdal           String?   @db.Char(50)
  perbelgedurumu      String?   @db.Char(50)
  permeslek           String?   @db.Char(50)
  perservis           String?   @db.Char(50)
  perdurak            String?   @db.Char(50)
  per_al__ma_ekli     String?   @map("perçalışmaşekli") @db.Char(50)
  per_zeldurum        String?   @map("perözeldurum") @db.Char(80)
  per_cret            Decimal?  @map("perücret") @db.Decimal(20, 6)
  peragioran_         String?   @map("peragioranı") @db.VarChar(5)
  peragitutar         String?   @db.VarChar(6)
  perisge_itim        String?   @map("perisgeğitim") @db.Char(50)
  perperiyodikmuayene String?   @db.Char(50)
  perilkyard_mbelgesi DateTime? @map("perilkyardımbelgesi") @db.Date
  peracildurumgrubu   String?   @db.Char(50)
  perhijyenbelgesi    Int?      @db.TinyInt
  perbanka            String?   @db.Char(50)
  perbankahesapno     String?   @db.Char(50)
  peribanno           String?   @db.Char(50)
  perfirma            String?   @db.Char(50)
  pervesika           String?   @db.Char(50)
  perFoto             String?   @db.Char(50)
  duzey               String?   @db.Char(50)
  perk_mgv            Decimal?  @default(0.00000) @map("perkümgv") @db.Decimal(5, 5)
  perk_mmat           Decimal?  @default(0.00000) @map("perkümmat") @db.Decimal(5, 5)
  per_ube             String?   @map("perşube") @db.Char(10)
  peristisnahaftasonu Int?      @default(0)
  percumamola         Int?      @default(0)
  perayar             String?   @default("1111111111") @db.Char(10)
  aktif               String?   @default("1") @db.VarChar(1)
  hs                  Int       @default(0)
  s                   Int       @default(0) @map("şs")
  peryetki            String    @default("yok") @db.Char(50)
  pertwitter          String?   @db.Char(50)
  perinstagram        String?   @db.Char(50)
  perfacebook         String?   @db.Char(50)
  peryoutube          String?   @db.Char(50)
  perlinkedin         String?   @db.Char(50)
  admin               Int       @default(0)
  kartokut            Int       @default(1)
  pervardiya          Int?      @default(1)
}

model personel_personeller_aday {
  id                  Int       @unique(map: "id") @default(autoincrement())
  perad               String?   @db.Char(15)
  persoyad            String?   @db.Char(15)
  peradsoyad          String?   @db.Char(50)
  pertcno             String?   @db.Char(11)
  percinsiyet         String?   @db.Char(15)
  peruyru_u           String?   @map("peruyruğu") @db.Char(10)
  perbabaad_          String?   @map("perbabaadı") @db.Char(15)
  perceptelefonu      String?   @db.VarChar(20)
  pereposta           String?   @db.Char(39)
  perdo_umtarihi      DateTime? @map("perdoğumtarihi") @db.Date
  perdo_umyeri        String?   @map("perdoğumyeri") @db.Char(20)
  peraskerlik         String?   @db.Char(10)
  permedenihali       String?   @db.Char(10)
  pere_durumu         String?   @map("pereşdurumu") @db.Char(10)
  per_ocuk            String?   @map("perçocuk") @db.Char(1)
  per__renimdurumu    String?   @map("peröğrenimdurumu") @db.Char(50)
  permezunokul        String?   @db.Char(50)
  permezunokulb_l_m   String?   @map("permezunokulbölüm") @db.Char(50)
  peri_egiri_tarihi   DateTime? @map("perişegiriştarihi") @db.Date
  peradres            String?   @db.Char(200)
  peril               String?   @db.Char(15)
  peril_e             String?   @map("perilçe") @db.Char(50)
  perkangrubu         String?   @db.Char(15)
  perk_s_m            String?   @map("perkısım") @db.Char(50)
  perb_l_m            String?   @map("perbölüm") @db.Char(50)
  perbirim            String?   @db.Char(50)
  perbirimamiri       String?   @db.Char(50)
  perbirimamiriperkod String?   @db.Char(11)
  perbirimamiritcno   String?   @db.Char(11)
  perg_rev            String?   @map("pergörev") @db.Char(50)
  perehliyet          String?   @db.Char(10)
  persrc              String?   @db.Char(50)
  permykdurumu        String?   @db.Char(50)
  permykalan          String?   @db.Char(50)
  permykdal           String?   @db.Char(50)
  permeslek           String?   @db.Char(50)
  perservis           String?   @db.Char(50)
  perdurak            String?   @db.Char(30)
  per_al__ma_ekli     String?   @map("perçalışmaşekli") @db.Char(50)
  per_zeldurum        String?   @map("perözeldurum") @db.Char(50)
  per_cret            String?   @map("perücret") @db.Char(15)
  perfirma            String?   @db.Char(50)
  per_ube             String?   @map("perşube") @db.Char(10)
  perreferans         String?   @db.Char(50)
  yabanc_dil1         String?   @map("yabancıdil1") @db.Char(15)
  yabanc_dil1_derece  String?   @map("yabancıdil1_derece") @db.Char(10)
  yabanc_dil2         String?   @map("yabancıdil2") @db.Char(15)
  yabanc_dil2_derece  String?   @map("yabancıdil2_derece") @db.Char(10)
  yabanc_dil3         String?   @map("yabancıdil3") @db.Char(15)
  yabanc_dil3_derece  String?   @map("yabancıdil3_derece") @db.Char(10)
  bilgisayarderece    String?   @db.Char(10)
  ba_vurutarihi       DateTime? @map("başvurutarihi") @db.Date
  aktif               String    @default("0") @db.Char(1)
  sgkbildirim         String    @default("0") @db.Char(1)
}

model personel_puantaj {
  y_l             String?  @map("yıl") @db.VarChar(4)
  d_nem           String?  @map("dönem") @db.VarChar(7)
  perkod          String?  @db.Char(50)
  peradsoyad      String?  @db.Char(50)
  pertcno         String?  @db.VarChar(11)
  ngun            Float?
  hsonu           Float?
  bgun            Float?
  rtatil          Float?
  yizin           Float?
  saat            Float?
  fm              Float?
  hsfm            Float?
  hifm            Float?
  byfm            Float?
  rtfm            Float?
  istirahat       Int?
  rgunu           Int?
  dgunu           Int?
  eksikgun        Float?
  eksikneden      String?  @db.VarChar(2)
  avans           Float?
  netucret        Decimal? @db.Decimal(20, 6)
  agitutari       Float?
  fmtutari        Float?
  hstutari        Float?
  rttutari        Float?
  bytutari        Float?
  emtutari        Float?
  rkesinti        Float?
  aykesintisi     Float?
  besoran_        String?  @map("besoranı") @db.VarChar(1)
  bestutar_       Float?   @map("bestutarı")
  icra            Float?
  nafaka          Float?
  muafakat        Float?
  ot              Float?
  b_r_t           Float?   @map("bürüt")
  i_cissk         Float?   @map("işcissk")
  i_cii_sizlik    Float?   @map("işciişsizlik")
  vergimatrah_    Float?   @map("vergimatrahı")
  k_mvermat       Float?   @map("kümvermat")
  dv              Float?
  dvistisnas_     Float?   @map("dvistisnası")
  hesaplanandv    Float?
  gv              Float?
  istisnagv       Float?
  engelliverind   Float?
  istisnatoplam   Float?
  hesaplanangv    Float?
  kesintitoplam_  Float?   @map("kesintitoplamı")
  i_verenssk      Float?   @map("işverenssk")
  i_vereni_sizlik Float?   @map("işverenişsizlik")
  toplammaliyet   Float?
  k_mgv           Float?   @map("kümgv")
  kilit           Boolean? @default(false)
  beskontrol      Boolean? @default(false)
  id              Int      @unique(map: "id") @default(autoincrement())
}

model personel_rapor_onay {
  id                Int       @unique(map: "id") @default(autoincrement())
  pertcno           String?   @db.VarChar(11)
  peradsoyad        String?   @db.Char(50)
  raporKontrolTarih DateTime? @db.Date
  onay              Int?      @default(0) @db.UnsignedInt
}

model personel_sat_nalma {
  id                       Int       @unique(map: "id") @default(autoincrement())
  taleppertcno             String?   @db.Char(11)
  talepperadsoyad          String?   @db.Char(35)
  taleptarihi              DateTime? @db.Date
  talep_r_nad              String?   @map("talepürünad") @db.Char(80)
  talep_r_nfiyat           String?   @map("talepürünfiyat") @db.Char(10)
  talep_r_nadet            String?   @map("talepürünadet") @db.Char(10)
  talep_r_ntutar           String?   @map("talepürüntutar") @db.Char(10)
  talepa__klama            String?   @map("talepaçıklama") @db.Char(250)
  r_nlink                  String?   @map("ürünlink") @db.Char(250)
  talepa__klama_sat_nalama String?   @map("talepaçıklama_satınalama") @db.Char(250)
  yetkilia__klama          String?   @map("yetkiliaçıklama") @db.Char(250)
  onay                     Int?      @default(0)
  red                      Int?      @default(0)
  sil                      Int?      @default(0)
  g_r_ld_                  Int?      @default(0) @map("görüldü")
  demeid                   Int?      @map("ödemeid")

  @@map("personel_satınalma")
}

model personel_sat_nalma__deme {
  id          Int       @unique(map: "id") @default(autoincrement())
  isim        String    @default("0") @db.Char(50)
  tarih       DateTime? @db.Date
  adet        String?   @db.Char(80)
  tutar       String?   @db.Char(9)
  vadefark_   String?   @map("vadefarkı") @db.Char(10)
  a__klama    String?   @map("açıklama") @db.Char(250)
  demey_ntemi String?   @map("ödemeyöntemi") @db.Char(50)
  taksit      String?   @db.Char(50)
  onay        Int?      @default(0) @db.SmallInt

  @@map("personel_satınalma_ödeme")
}

model personel_site_izin {
  personel     String? @db.Char(50)
  sosyal_medya Int?    @default(1)
  profilfoto   Int?    @default(1)
  id           Int     @unique(map: "id") @default(autoincrement())
}

model personel_takip {
  username String? @db.Char(11)
  takip    String? @db.Char(11)
  onay     Int?    @default(0)
  id       Int     @unique(map: "id") @default(autoincrement())
}

model personel_web_duyuru {
  id            Int       @unique(map: "id") @default(autoincrement())
  duyurumetni   String    @db.VarChar(1000)
  duyuruba_l_k  String    @default("") @map("duyurubaşlık") @db.Char(250)
  nem           String    @default("") @map("önem") @db.Char(50)
  tarih         DateTime? @db.Date
  sontarih      DateTime? @db.Date
  perbirimamiri String?   @db.Char(50)
  nema__klama   String?   @map("önemaçıklama") @db.Char(50)
}

model personel_web_duyuru_okundu {
  id         Int       @unique(map: "id") @default(autoincrement())
  duyuruid   String?   @db.Char(50)
  tarih      DateTime? @db.Date
  saat       DateTime? @db.Time(0)
  pertcno    String?   @db.VarChar(11)
  peradsoyad String?   @db.VarChar(50)
}

model personel_yemek {
  tarih     DateTime? @db.Date
  orba      String?   @map("çorba") @db.Char(100)
  anayemek  String?   @db.Char(100)
  anayemek2 String?   @db.Char(100)
  ek        String?   @db.Char(100)
  ek2       String?   @db.Char(100)
  ek3       String?   @db.Char(100)
  id        Int       @unique(map: "id") @default(autoincrement())
}

model personel_yemek_anket {
  yemek    String?   @db.Char(50)
  t_r      String?   @map("tür") @db.Char(20)
  yemek_id Int?
  oy       Int?
  personel String?   @db.Char(50)
  tarih    DateTime? @db.Date
  id       Int       @unique(map: "id") @default(autoincrement())
}

model personel_yemek_tan_mlar {
  yemek String? @db.Char(50)
  t_r   String? @map("tür") @db.Char(50)
  id    Int     @unique(map: "id") @default(autoincrement())

  @@map("personel_yemek_tanımlar")
}

model personel_zil {
  id             Int     @unique(map: "id")
  zil            String? @db.Char(50)
  Pazartesigir   String? @db.Char(5)
  Pazartesi__k   String? @map("Pazartesiçık") @db.Char(5)
  Sal_gir        String? @map("Salıgir") @db.Char(5)
  Sal___k        String? @map("Salıçık") @db.Char(5)
  ar_ambagir     String? @map("Çarşambagir") @db.Char(5)
  ar_amba__k     String? @map("Çarşambaçık") @db.Char(5)
  Per_embegir    String? @map("Perşembegir") @db.Char(5)
  Per_embe__k    String? @map("Perşembeçık") @db.Char(5)
  Cumagir        String? @db.Char(5)
  Cuma__k        String? @map("Cumaçık") @db.Char(5)
  Cumartesigir   String? @db.Char(5)
  Cumartesi__k   String? @map("Cumartesiçık") @db.Char(5)
  Pazargir       String? @db.Char(5)
  Pazar__k       String? @map("Pazarçık") @db.Char(5)
  Pazartesi      String? @db.Char(40)
  Sal_           String? @map("Salı") @db.Char(40)
  ar_amba        String? @map("Çarşamba") @db.Char(40)
  Per_embe       String? @map("Perşembe") @db.Char(40)
  Cuma           String? @db.Char(40)
  Cumartesi      String? @db.Char(40)
  Pazar          String? @db.Char(40)
  k_rm_z__kaz    String? @map("kırmızıİkaz") @db.Char(40)
  sar__kaz       String? @map("sarıİkaz") @db.Char(40)
  siyah_kaz      String? @map("siyahİkaz") @db.Char(40)
  istiklalmar__  String? @map("istiklalmarşı") @db.Char(40)
  sayg_duru_u    String? @map("saygıduruşu") @db.Char(40)
  tek            String? @db.Char(40)
  zel            String? @map("özel") @db.Char(40)
  ayarla_tarih   String? @db.Char(40)
  ayarla_saat    String? @db.Char(40)
  ayarla_kontrol String? @db.Char(7)
  ayarla_zil     String? @db.Char(40)
  zil_simleri    String? @map("zilİsimleri") @db.Char(250)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model puantaj {
  y_l             String?  @map("yıl") @db.VarChar(4)
  d_nem           String?  @map("dönem") @db.VarChar(7)
  perkod          String?  @db.Char(50)
  peradsoyad      String?  @db.Char(50)
  pertcno         String?  @db.VarChar(11)
  ngun            Float?
  hsonu           Float?
  bgun            Float?
  rtatil          Float?
  yizin           Float?
  saat            Float?
  fm              Float?
  hsfm            Float?
  hifm            Float?
  byfm            Float?
  rtfm            Float?
  istirahat       Int?
  rgunu           Int?
  dgunu           Int?
  eksikgun        Float?
  eksikneden      String?  @db.VarChar(2)
  avans           Float?
  netucret        Float?
  agitutari       Float?
  fmtutari        Float?
  hstutari        Float?
  rttutari        Float?
  bytutari        Float?
  emtutari        Float?
  rkesinti        Float?
  aykesintisi     Float?
  besoran_        String?  @map("besoranı") @db.VarChar(1)
  bestutar_       Float?   @map("bestutarı")
  icra            Float?
  nafaka          Float?
  muafakat        Float?
  ot              Float?
  b_r_t           Float?   @map("bürüt")
  i_cissk         Float?   @map("işcissk")
  i_cii_sizlik    Float?   @map("işciişsizlik")
  vergimatrah_    Float?   @map("vergimatrahı")
  k_mvermat       Float?   @map("kümvermat")
  dv              Float?
  dvistisnas_     Float?   @map("dvistisnası")
  hesaplanandv    Float?
  gv              Float?
  istisnagv       Float?
  engelliverind   Float?
  istisnatoplam   Float?
  hesaplanangv    Float?
  kesintitoplam_  Float?   @map("kesintitoplamı")
  i_verenssk      Float?   @map("işverenssk")
  i_vereni_sizlik Float?   @map("işverenişsizlik")
  toplammaliyet   Float?
  k_mgv           Float?   @map("kümgv")
  kilit           Boolean? @default(false)
  beskontrol      Boolean? @default(false)
  id              Int?     @unique(map: "id")

  @@ignore
}

model serverKontrol {
  cihazAd_           String? @map("cihazAdı") @db.Char(50)
  de_i_ken           Int?    @default(0) @map("değişken")
  ba_lant_           String? @map("bağlantı") @db.Char(1)
  perM_dDevSms       Int?    @default(0) @map("perMüdDevSms")
  perBirimAmirDevSms Int?    @default(0)
  perAySonuHatSms    Int?    @default(0)
  clientKontrolSms   Int?    @default(0)
  KontrolSms         Int?    @default(0)
  id                 Int     @unique(map: "id") @default(autoincrement())
}

model sessions {
  session_id String  @id @db.VarChar(128)
  expires    Int     @db.UnsignedInt
  data       String? @db.MediumText
}

model tanim_belgeler {
  id          Int     @unique(map: "id") @default(autoincrement())
  belge       String? @db.Char(50)
  belgeAd_    String? @map("belgeAdı") @db.Char(50)
  belgeuzant_ String  @map("belgeuzantı") @db.Char(50)
}

model tanim_mola {
  id        Int     @unique(map: "id") @default(autoincrement())
  mola1_bas String? @db.VarChar(5)
  mola1_son String? @db.VarChar(5)
  mola2_bas String? @db.VarChar(5)
  mola2_son String? @db.VarChar(5)
  mola3_bas String? @db.VarChar(5)
  mola3_son String? @db.VarChar(5)
  mola4_bas String? @db.VarChar(5)
  mola4_son String? @db.VarChar(5)
  mola5_bas String? @db.VarChar(5)
  mola5_son String? @db.VarChar(5)
  vardiya1  String? @db.VarChar(5)
  vardiya2  String? @db.VarChar(5)
  tan_m     String? @map("tanım") @db.Char(10)
}

model tanim_my {
  my_durum String? @db.VarChar(50)
  durum    String? @db.VarChar(50)
  id       Int     @unique(map: "id") @default(autoincrement())
}

model tanim_servis {
  tanim         String? @db.VarChar(50)
  servis_ismi   String? @db.VarChar(50)
  koltuk_say_s_ String? @map("koltuk_sayısı") @db.VarChar(50)
  durak1        String? @db.VarChar(50)
  durak2        String? @db.VarChar(50)
  durak3        String? @db.VarChar(50)
  durak4        String? @db.VarChar(50)
  durak5        String? @db.VarChar(50)
  durak6        String? @db.VarChar(50)
  durak7        String? @db.VarChar(50)
  durak8        String? @db.VarChar(50)
  durak9        String? @db.VarChar(50)
  id            Int     @unique(map: "id") @default(autoincrement())
}

model tanim_tatil {
  tarih    DateTime? @db.Date
  tatil    String?   @db.VarChar(50)
  aciklama String?   @db.VarChar(200)
  puantaj  String?   @db.Char(100)
  id       Int       @unique(map: "id") @default(autoincrement())
}

model z_bo__tablo {
  y_l    String? @map("yıl") @db.Char(4)
  ay     String? @db.Char(2)
  incele String? @db.Char(2)
  id     Int     @unique(map: "id") @default(autoincrement())

  @@map("z_boş_tablo")
}
